import request from "supertest"
import app from "../app"
import { prisma } from "../config/db"

describe("Order Routes", () => {
  it("should have tests for order routes", () => {
    expect(true).toBe(true)
  })
})
