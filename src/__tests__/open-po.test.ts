import request from "supertest"
import app from "../app"
import { prisma } from "../config/db"

describe("Open PO Routes", () => {
  it("should have tests for open-po routes", () => {
    expect(true).toBe(true)
  })
})
