import request from "supertest"
import app from "../app"
import { prisma } from "../config/db"

describe("Store Routes", () => {
  it("should have tests for store routes", () => {
    expect(true).toBe(true)
  })
})
