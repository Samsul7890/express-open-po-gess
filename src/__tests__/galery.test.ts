import request from "supertest"
import app from "../app"
import { prisma } from "../config/db"

describe("Galery Routes", () => {
  it("should have tests for galery routes", () => {
    expect(true).toBe(true)
  })
})
