import request from "supertest"
import app from "../app"
import { prisma } from "../config/db"

describe("Product Routes", () => {
  it("should have tests for product routes", () => {
    expect(true).toBe(true)
  })
})
